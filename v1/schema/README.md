# v1 / schema

**Status:** Draft — this directory will hold versioned **JSON Schemas** and related machine-readable models for MCP and API integration.

Add schema files here as the specification hardens. Until then, treat this tree as a scaffold only.
