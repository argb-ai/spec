# v1 / transport

**Status:** Draft — this directory will hold physical-layer and framing definitions (e.g. serial/USB framing, discovery, and hardware identification flows).

**Non-goals for this folder:** high-level “vibe” semantics — those live under `concepts.md` and `../schema/`.
